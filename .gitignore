# Python cache
__pycache__/
*.py[cod]
*.pyo

# NOTE: predictions.csv and results.json ARE the submission artifacts and
# MUST be committed. The original upstream .gitignore listed them for the
# applicant's dev convenience; we need them tracked for the SMILES-2026
# submission contract.

# HuggingFace model cache
.cache/

# Stray top-level debug logs from interactive sessions
run*.log

# Virtual environments
.venv/
venv/
env/

# Jupyter notebooks checkpoints
.ipynb_checkpoints/

# OS files
.DS_Store
Thumbs.db

# Local credentials (wandb personal account, etc.) -- never commit
.env
.env.local

# wandb local run cache
wandb/

.env # for wandb